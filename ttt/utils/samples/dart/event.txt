abstract class $NAMING$Event {
  void send$NAMING$Event() {
    try {
      on$NAMING$EventDone(null);
    } catch (e) {
      on$NAMING$EventDone(e);
    }
  }

  void on$NAMING$EventDone(dynamic e);
}

